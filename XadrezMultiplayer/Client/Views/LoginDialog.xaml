<Window x:Class="Client.Views.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="200" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Username:"/>
        <TextBox Grid.Row="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Content="Password:" Grid.Row="2"/>
        <TextBox Grid.Row="3" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Content="Login" Grid.Row="4" Command="{Binding LoginCommand}" Margin="0,10,0,0"/>
        <TextBlock Grid.Row="5" Text="{Binding Status}" Margin="0,10,0,0"/>
    </Grid>
</Window>